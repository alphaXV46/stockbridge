<Window x:Class="StockBridge_XAML.UpdateStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventory Adjustment" Height="520" Width="420" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F1F5F9">

    <Window.Resources>
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ModernInputStyle" TargetType="Control">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Border Background="White" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#000000" Opacity="0.1"/>
            </Border.Effect>

            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <Border Background="#EFF6FF" CornerRadius="6" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <TextBlock Text="STOCK ADJUSTMENT" FontSize="10" FontWeight="Black" Foreground="#3B82F6" Margin="8,4"/>
                    </Border>
                    <TextBlock x:Name="lblProductName" Text="PRODUCT NAME" FontSize="18" FontWeight="Black" Foreground="#1E293B" TextWrapping="Wrap"/>
                    <Rectangle Height="1" Fill="#F1F5F9" Margin="0,15,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="TIPE TRANSAKSI" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox x:Name="cbTransactionType" Margin="0,0,0,18" Style="{StaticResource ModernInputStyle}">
                        <ComboBoxItem Content="Barang Masuk" IsSelected="True"/>
                        <ComboBoxItem Content="Barang Keluar"/>
                    </ComboBox>

                    <TextBlock Text="SATUAN" Style="{StaticResource FormLabelStyle}"/>
                    <ComboBox x:Name="cbUnitType" Margin="0,0,0,18" Style="{StaticResource ModernInputStyle}">
                        <ComboBoxItem x:Name="itemUnit" Content="Loading..."/>
                        <ComboBoxItem x:Name="itemPack" Content="Loading..."/>
                    </ComboBox>

                    <TextBlock Text="JUMLAH QUANTITY" Style="{StaticResource FormLabelStyle}"/>
                    <TextBox x:Name="txtQuantity" Text="1" FontSize="18" FontWeight="Bold" Foreground="#3B82F6" 
                             Style="{StaticResource ModernInputStyle}" Padding="10,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <Button Content="SIMPAN PERUBAHAN" Click="BtnProcess_Click" Height="50"
                            Background="#3B82F6" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>