<Page x:Class="StockBridge_XAML.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Inventory Management"
      d:DesignHeight="750" d:DesignWidth="1100"
      Background="#F8FAFC">

    <Page.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Opacity="0.1" Color="Black" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Inventory Repository" FontSize="26" FontWeight="Black" Foreground="#0F172A"/>
                <TextBlock Text="Centralized supplier asset management system" Foreground="#64748B" FontSize="13" Margin="0,4,0,0"/>
            </StackPanel>

            <Button x:Name="btnAddProduct" Grid.Column="1" Content="+ Add New SKU" Style="{StaticResource PrimaryButton}" Click="BtnAddProduct_Click" Width="160"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Margin="0,0,0,20" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid Margin="15">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#F1F5F9" CornerRadius="8" Width="300">
                        <Grid Margin="10,5">
                            <TextBlock x:Name="Placeholder" Text="🔍 Search products..." Foreground="#94A3B8" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtSearch" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" TextChanged="TxtSearch_TextChanged"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnUpdateStock" Content="⚙ Adjust Stock" Click="BtnUpdateStock_Click" Background="Transparent" Foreground="#475569" FontWeight="Bold" Margin="0,0,15,0" BorderThickness="0" Cursor="Hand"/>
                    <Button x:Name="btnPrintQR" Content="⎙ QR Label" Click="BtnPrintQR_Click" Background="#F1F5F9" Foreground="#1E293B" FontWeight="Bold" BorderThickness="0" Width="120" Height="35">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource StatCardStyle}">
            <DataGrid x:Name="dgProducts" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      RowHeight="65"
                      Background="Transparent"
                      BorderThickness="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="PRODUCT IDENTITY" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10,0">
                                    <Border Width="35" Height="35" Background="#DBEAFE" CornerRadius="8">
                                        <TextBlock Text="📦" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding product_name}" FontWeight="Bold" Foreground="#1E293B"/>
                                        <TextBlock Text="{Binding id}" Foreground="#94A3B8" FontSize="10"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="STOCK" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F0FDF4" CornerRadius="15" Padding="10,5" HorizontalAlignment="Left" Margin="10,0">
                                    <TextBlock Text="{Binding base_stock}" Foreground="#166534" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="MODIFIED" Binding="{Binding created_at, StringFormat=\{0:dd MMM\}}" Width="100"/>
                    <DataGridTextColumn Header="NOTES" Binding="{Binding description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>