<Page x:Class="StockBridge_XAML.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Transaction Logs"
      d:DesignHeight="750" d:DesignWidth="1100"
      Background="#F8FAFC">

    <Page.Resources>
        <Style x:Key="ModernSearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushBase}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="5,0"/>
                                <TextBlock x:Name="placeholder" Text="Search by Product ID or SKU..." 
                                           Foreground="{StaticResource TextMutedBrush}" FontStyle="Italic"
                                           VerticalAlignment="Center" Margin="15,0" IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BorderBrushActive}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="35">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="Logistik &amp; Riwayat Transaksi" FontSize="28" FontWeight="Black" Foreground="{StaticResource DeepNavyBrush}"/>
                <TextBlock Text="Audit log otomatis untuk seluruh pergerakan inventaris StockBridge." 
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearchLogs" Grid.Column="0" MaxWidth="400" HorizontalAlignment="Left" Width="400"
                      Style="{StaticResource ModernSearchBox}"/>

            <ComboBox Grid.Column="1" Margin="15,0" Width="180" Height="45" VerticalContentAlignment="Center" 
                      Padding="10,0">
                <ComboBoxItem IsSelected="True" Content="All Activities"/>
                <ComboBoxItem Content="Stock In (Masuk)"/>
                <ComboBoxItem Content="Stock Out (Keluar)"/>
            </ComboBox>

            <Button Grid.Column="2" Content="Export CSV Report" Style="{StaticResource SecondaryButton}" Width="180"/>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="20" BorderBrush="{StaticResource BorderBrushBase}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="4" Color="{StaticResource ShadowColor}" Opacity="0.04"/>
            </Border.Effect>

            <Grid Margin="15">
                <DataGrid x:Name="dgLogs" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="Transparent" BorderThickness="0" GridLinesVisibility="None"
                          HeadersVisibility="Column" RowHeight="60">

                    <DataGrid.Columns>
                        <!-- Right-aligned Date/Time -->
                        <DataGridTextColumn Header="TIME" Binding="{Binding created_at, StringFormat=\{0:HH:mm - dd MMM yyyy\}}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,20,0"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Left-aligned SKU -->
                        <DataGridTemplateColumn Header="SKU / PRODUCT ID" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                                        <Border Width="8" Height="8" CornerRadius="4" Background="{StaticResource BrandBlueBrush}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding product_id}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Center-aligned Action status badge -->
                        <DataGridTemplateColumn Header="ACTION TYPE" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border x:Name="TypeBadge" CornerRadius="8" Padding="12,5" HorizontalAlignment="Center" Margin="10,0">
                                        <TextBlock x:Name="TypeText" Text="{Binding type}" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding type}" Value="IN">
                                            <Setter TargetName="TypeBadge" Property="Background" Value="{StaticResource SuccessLightBrush}"/>
                                            <Setter TargetName="TypeText" Property="Foreground" Value="{StaticResource SuccessTextBrush}"/>
                                            <Setter TargetName="TypeText" Property="Text" Value="▲ STOCK IN"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding type}" Value="OUT">
                                            <Setter TargetName="TypeBadge" Property="Background" Value="{StaticResource DangerLightBrush}"/>
                                            <Setter TargetName="TypeText" Property="Foreground" Value="{StaticResource DangerTextBrush}"/>
                                            <Setter TargetName="TypeText" Property="Text" Value="▼ STOCK OUT"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Right-aligned numeric quantity -->
                        <DataGridTextColumn Header="QTY" Binding="{Binding quantity}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,20,0"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Right-aligned final balance -->
                        <DataGridTemplateColumn Header="FINAL BALANCE" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding total_affected}" VerticalAlignment="Center" HorizontalAlignment="Right"
                                               Margin="0,0,25,0" Foreground="{StaticResource BrandBlueBrush}" FontWeight="Bold" FontSize="14"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>